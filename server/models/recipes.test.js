const { getById, list, add, edit, deleteById } = require('./recipes.js');

test('tests should be working', () => {
  expect(1).toBe(1);
});

test("should be able to add and remove recipes", () => {

})